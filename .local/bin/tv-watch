#!/usr/bin/env zsh
name="$*"

file=$(rg -l "^Name=$name$" ~/.local/share/applications/*.desktop 2>/dev/null | head -1)

[ -z "$file" ] && exit 1

cmd=$(rg '^Exec=' "$file" 2>/dev/null | sed 's/^Exec=//')

exec zsh -c "$cmd"
